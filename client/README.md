# RC Client

Статический браузерный клиент. Откройте `index.html` через локальный сервер или любой статический хостинг.

## Быстрый старт

```bash
python -m http.server 5173
```

Откройте `http://localhost:5173?api=http://localhost:3001` для работы с локальным сервером без reverse proxy.
